<% 
	add_body_class "category", "discussions", "category#{@category.id}"
	@page_title = @category.name 
%>

<div class="functions">
	<%= link_to "New Discussion +", new_discussion_path, :class => :create %>
</div>
<h2 class="section"><%= link_to "Categories", categories_path %> &raquo; <%= link_to @category.name, category_path(@category) %></h2>

<%= render :partial => 'discussions/table', :locals => {:discussions => @discussions} %>
