<%
  @page_title = "New #{@exchange.class.to_s.downcase}"
%>

<div class="page_content">
  <%= form_for(@exchange) do |f| %>
    <%= render partial: "exchanges/form", locals: { f: f } %>
    <p>
      <%= submit_tag "Create discussion" %>
    </p>
  <% end %>
</div>
