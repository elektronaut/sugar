<% content_for(:body_class, "participated discussions") %>
<% content_for(:page_title, "#{@user.username}'s Discussions") %>

<%= render partial: "exchanges/table", locals: { exchanges: @discussions } %>
