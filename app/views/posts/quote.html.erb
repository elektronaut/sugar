<blockquote><cite>Posted by <%= link_to h(@post.user.username), paged_discussion_path(:id => @post.exchange, :page => @post.page, :anchor => "post-#{@post.id}") %>:</cite><%= meify(@post.body, @post.user) %></blockquote>
